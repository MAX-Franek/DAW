services:
  base:
    build:
      #ruta a través de la cual desde cualquier proyecto se tiene acceso a cualquier imagen (Dfile), recurso
      context: /home/fran/DAW/Docker/Caronte
      dockerfile: ./dockerfiles/base/ubuntu/Dockerfile